import React from 'react'
import AppNavigator from './components/navigator/app_navigator'

export default class Root extends React.Component {
    render() {
        return (
            <AppNavigator />
        )
    }
}